export class MonthModel {
    constructor (
        public no: string,
        public mon: string
    ) {}
}

export class GLCostModel {
    constructor (
        public gl: string
    ) {}
}

export class UnitsModel {
    constructor (
        public unit: string
    ) {}
}

export class SuppliersModel {
    constructor (
        public sp: string
    ) {}
}
